{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle Categorie</th>
                <td>{{ categorie.libelleCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}"><button class="btn btn-outline-info">Voir la liste</button></a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}"><button class="btn btn-outline-warning">editer</button></a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
