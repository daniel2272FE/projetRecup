{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle Categorie</th>
                <td>{{ categorie.libelleCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}">Voir la liste</a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}">editer</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
