{% extends 'base.html.twig' %}

{% block title %}Tarif{% endblock %}

{% block body %}
    <h1>Tarif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarif.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ tarif.designation }}</td>
            </tr>
            <tr>
                <th>Prix Unitaire Fixe</th>
                <td>{{ tarif.prixUnitaireFixe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tarif_index') }}"><button class="btn btn-outline-info">Voir la liste</button></a>

    <a href="{{ path('tarif_edit', {'id': tarif.id}) }}"><button class="btn btn-outline-warning">editer</button></a>

    {{ include('tarif/_delete_form.html.twig') }}
{% endblock %}
